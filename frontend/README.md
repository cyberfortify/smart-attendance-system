# React + Vite

# from frontend/
npm init vite@latest frontend --template react
cd frontend
npm install
npm install axios react-router-dom chart.js react-chartjs-2
# Tailwind setup (follow official tailwind + vite guide)
